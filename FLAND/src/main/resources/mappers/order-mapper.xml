<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="order">
	<insert id="add">
		insert into order(no, orderNo, company, serial, orderDate, endDate, fabric, workWidth, workWeight, price, color, orderLength, colorBt)
		values(seq_order.nextval,
			#{orderNo},
			#{company},
			#{serial},
			#{orderDate},
			#{endDate},
			#{fabric},
			#{workWidth},
			#{workWeight},
			#{price},
			#{color},
			#{orderLength},
			#{colorBt},
		)
	</insert>
</mapper>